#ifndef FILL_H
#define FILL_H

#include <string>
#include <vector>
#include <iostream>

std::string::size_type maxlen(const std::vector<std::string> &);
std::vector<std::string> center(const std::vector<std::string> &);

#endif // FILL_H
